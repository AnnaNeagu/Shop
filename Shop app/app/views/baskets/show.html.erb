<!DOCTYPE html>
<html>
<meta name="viewport" content="width=device-width, initial-scale=1">
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<script src="https://kit.fontawesome.com/35eb3c981f.js" crossorigin="anonymous"></script>
<body style="background-color: #f3ead8">
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto topnav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/products">Products</a>
        </li> 
         <li class="nav-item">
              <a href="/products/new"><i class="fa fa-plus fa-xl " ></i></a>
       </li>     
      </ul>           
          <i class="fa-solid fa-basket-shopping fa-xl " ></i></a>     
    </div>
  </div>
</nav>
<div class= "container" >
<h1 class="display-1" style="text-align:center;">Basket</h1>    
        <div class="container"> 
          <div class="row ">       
            <div class="col-sm-6" >             
                        <% @order_items.each do |item| %>
                            <h3><%= item.product.name %></h3>
                            <h3><%= item.product.price %> RON</h3>
                            <div class="col-sm-3">
                            <%= form_for item do |f|%>
                            <%= f.hidden_field :product_id, :value => item.product.bar_code%>
                            <%= f.number_field :quantity, :value => item.quantity.to_i, class:"form-control" %>
                            <%# <%= f.submit "Update"%> 
                            <br />
                             <%= button_to "Delete", order_item_path(item), method: :delete, class: "btn btn-outline-dark"  %>
                             <% end %>
                           </div>
                            <%# <%= button_to "Update", order_item_path(item), method: :update%> 
                           
                            <h2>Subtotal: <%= item.total%> RON </h2>
                            <p>----------------------------------</p>
                        <% end %>
            </div>
            <div class="col-sm-6"  >
           <h3> Your current value has a total of: <%=current_order.subtotal %> RON </h3>
                <div class=" col-sm-6">
                    <label for="floatingInput">Discount</label>
                    <input type="text" class="form-control"  placeholder="code discount">
                    <%# <%= @discount %>
                         <%# <%= text_field(@discount, :class => "form-control" )%>
                    <br />
                    <button type="button" class="btn btn-outline-dark">Apply</button>
                </div>
            
              </div>               
           </div>
        </div>
      </div>
           
  </div>
</div>
</body>
</html>