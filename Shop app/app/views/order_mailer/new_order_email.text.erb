

Order Details:
--------------------------
 
   <%total = 0%>
     <%subtotal = 0%>
     <%price = 0%>
     <% @order_item.each do |item|  %>
     <% product = Product.find_by_id(item.product_id)%>
     <%= product.name %> 
          Price:<%= item.unit_price %> RON
          Quantity:<%= item.quantity %> 
          Total price per product:<%= price = item.quantity * item.unit_price %> RON
    <% total = total + price %> 
    <%# <% discount = item.discount_percentage %>  
      Discount : <%= item.order.discount.name %>
    <% end %>
  
    <%# <% byebug %> 
   
    
                                       Total to pay <%= total%> RON
    

